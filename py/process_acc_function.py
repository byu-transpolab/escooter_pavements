# revised data processor for acceleration data
import numpy as np
from scipy import signal

def get_accel_from_folder(folder):
  # this is how you define a function in python
  
  # first, let's read in the file
  # read the csv into whatever dr mazzeo had
  
  # then, do the signal processing
  # using signal in scipy
  
  # finally, return the data table into R
